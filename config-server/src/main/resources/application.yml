server:
  port: 8096

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Jzedy/example-of-springcloud
          search-paths: config
          default-label: master
#          username:
#          password:
